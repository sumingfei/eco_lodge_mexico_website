# Public site URL (used for canonical URLs, sitemap, robots and structured data)
NEXT_PUBLIC_SITE_URL=https://habitar.mx

# Where leads from the estimator / contact / developers forms are POSTed as JSON.
# Leave empty to log leads to the server console during development.
LEAD_WEBHOOK_URL=
